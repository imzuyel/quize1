# PGTSC কুইজ অ্যারেনা — ডিপ্লয়মেন্ট গাইড

## ⚠️ লাইভ করার আগে অবশ্যই

| # | কাজ | কেন |
|---|---|---|
| ১ | `DEMO_MODE=false` সেট করুন | না করলে **যে কেউ পাসওয়ার্ড ছাড়াই অ্যাডমিন প্যানেলে ঢুকতে পারবে** |
| ২ | আসল `DATABASE_URL` দিন | ডেটা স্থায়ীভাবে সংরক্ষণের জন্য |
| ৩ | `setup-production.ts` চালান | ডেমো ডেটা ছাড়া পরিষ্কার শুরু |

---

## ধাপ ১ — ডেটাবেস (XAMPP MySQL)

1. XAMPP Control Panel খুলুন
2. **MySQL → Start** চাপুন
3. `http://localhost/phpmyadmin` খুলুন
4. **New** → database name `quiz_arena` → **Create**

অথবা MySQL console/phpMyAdmin-এ:

```sql
CREATE DATABASE quiz_arena CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

এই project-এর schema Drizzle দিয়ে তৈরি হবে; আলাদা MySQL লাগবে না।

## ধাপ ২ — এনভায়রনমেন্ট

`.env.example` কপি করে `.env` বানান:

```bash
DATABASE_URL=mysql://root:@127.0.0.1:3306/quiz_arena   # আবশ্যক
DEMO_MODE=false                 # আবশ্যক (লাইভে)
GEMINI_API_KEY=AIza...          # ⭐ ফ্রি — প্রশ্নের মান অনেক বাড়ে
```

### 🆓 ফ্রি AI কী কোথায় পাবেন

| প্রোভাইডার | ফ্রি? | কার্ড লাগে? | লিংক |
|---|---|---|---|
| **Google Gemini** ⭐ | স্থায়ী ফ্রি টিয়ার | ❌ না | [aistudio.google.com/apikey](https://aistudio.google.com/apikey) |
| Groq | ফ্রি টিয়ার | ❌ না | [console.groq.com](https://console.groq.com/keys) |
| OpenAI | সীমিত ক্রেডিট | ✅ হ্যাঁ | platform.openai.com |
| Anthropic | সীমিত ক্রেডিট | ✅ হ্যাঁ | console.anthropic.com |

**একাধিক কী দিতে পারেন** — অ্যাপ ক্রমানুসারে চেষ্টা করে (Gemini → Groq → OpenAI → Anthropic)।
কোনোটি বন্ধ, রেট-লিমিটেড বা ক্রেডিট শেষ হলে **স্বয়ংক্রিয়ভাবে পরেরটি** ব্যবহার হয়।
সবগুলো ব্যর্থ হলে তবেই বিল্ট-ইন জেনারেটর চালু হয় — প্ল্যাটফর্ম কখনো থেমে যায় না।

যাচাই করা মডেল: `gemini-3.5-flash`, `gemini-3-flash-preview` (Gemini) ·
`openai/gpt-oss-120b` (Groq)। পুরোনো `gemini-2.x` ও `llama-3.x` নাম আর কাজ করে না।

ওভাররাইড: `AI_PROVIDER=groq` (কোনটি আগে) · `AI_MODEL=...` (নির্দিষ্ট মডেল)।

> ফ্রি টিয়ারে প্রতি মিনিটে ১০-১৫টি রিকোয়েস্টের সীমা থাকে। অ্যাপ নিজেই ব্যাচ
> ধীরে পাঠায় ও 429 পেলে অপেক্ষা করে পুনরায় চেষ্টা করে, তাই ১০০০ প্রশ্নও
> তৈরি হবে — শুধু একটু সময় নেবে।

---

## ধাপ ৩ — টেবিল ও অ্যাডমিন

```bash
npm install
npx drizzle-kit push                                    # টেবিল তৈরি
npx tsx scripts/setup-production.ts admin@pgtsc.edu.bd 'StrongPass123'
```

এতে তৈরি হবে: শ্রেণি ৬-১২, সেকশন, ৪টি ট্রেড, ১৬টি থিম, ৭টি অ্যাচিভমেন্ট, ১টি সুপার অ্যাডমিন।
**কোনো ভুয়া শিক্ষার্থী বা কুইজ নয়।**

---

## ধাপ ৪ — হোস্টিং

### বিকল্প ক — VPS (লাইভ কুইজের জন্য সুপারিশকৃত)

```bash
npm run build
npm run start          # অথবা: pm2 start npm --name pgtsc -- start
```

Hetzner / DigitalOcean-এ ~$৫-৬/মাস। Nginx দিয়ে HTTPS যোগ করুন।

### বিকল্প খ — Vercel

```bash
vercel --prod
```

Environment Variables-এ `DATABASE_URL` ও `DEMO_MODE=false` দিন।

> **⚠️ সীমাবদ্ধতা:** লাইভ কুইজের রিয়েল-টাইম ইঞ্জিন সার্ভারের মেমোরিতে চলে। Vercel একাধিক ইনস্ট্যান্স চালালে ভিন্ন সার্ভারে থাকা শিক্ষার্থীরা সিঙ্ক হারাবে।
> কুইজ, পরীক্ষা, অনুশীলন, রিপোর্ট — সব ঠিক চলবে। **শুধু লাইভ মাল্টিপ্লেয়ারের জন্য এক-ইনস্ট্যান্স VPS ব্যবহার করুন,** অথবা Redis অ্যাডাপ্টার যোগ করান।

---

## ধাপ ৫ — লাইভের পর যাচাই

```bash
curl https://your-domain/api/health          # {"ok":true}
```

- [ ] `/teacher` এ গেলে `/login` এ পাঠায় (ডেমো বাইপাস বন্ধ)
- [ ] `/demo?next=/admin` → `/login` এ পাঠায়
- [ ] অ্যাডমিন অ্যাকাউন্টে লগইন হয়
- [ ] অ্যাডমিন → ইউজার → শিক্ষক/শিক্ষার্থী যোগ (পাসওয়ার্ড স্বয়ংক্রিয়ভাবে তৈরি হয়ে একবার দেখানো হয়)

---

## ক্ষমতা ও সীমা

| বিষয় | অবস্থা |
|---|---|
| প্রশ্ন ব্যাংক, কুইজ, পরীক্ষা, রিপোর্ট | সীমাহীন (ডেটাবেস নির্ভর) |
| লাইভ কুইজ (এক VPS) | ~৫০০-১০০০ জন |
| লাইভ কুইজ (মাল্টি-ইনস্ট্যান্স) | Redis ছাড়া চলবে না |
| AI প্রশ্ন (কী সহ) | ১০০০+ প্রশ্ন, ব্যাকগ্রাউন্ড জব |
| স্ক্যান করা PDF | ❌ OCR নেই — টেক্সট-সহ PDF দিন |

---

## নিরাপত্তা

সেশন কুকি `httpOnly` + `sameSite=lax`, পাসওয়ার্ড `scrypt` হ্যাশড, সব স্কোরিং সার্ভার-সাইডে,
লগইন/নিবন্ধন/উত্তর/রিঅ্যাকশনে রেট লিমিট, সব API রুটে রোল যাচাই।

**AI কী কখনো ব্রাউজারে যায় না** — সব কল সার্ভার থেকে হয়।
